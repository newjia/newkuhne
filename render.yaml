services:
  - type: web
    name: kuhne-mcp-server
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      - key: DB_PATH
        value: orders.db
    healthCheckPath: /health
